//Generating hold screen

void sceneFive(PGraphics scene){  
  scene.beginDraw();
  scene.background(0,0,0);  
  scene.textAlign(CENTER);
  scene.textSize(40);
  scene.text("Generating...", w/2, h/2);
  scene.endDraw();
}

void defineGUIFive(){
}
